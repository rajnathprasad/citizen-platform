<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Scheme</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/editScheme.css">
</head>
<body>
    <div class="edit-scheme-container">
        <h2>Edit Scheme</h2>
        <form action="/admin/schemes/edit/<%= scheme._id %>" method="POST" class="edit-scheme-form">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" id="title" name="title" value="<%= scheme.title %>" required>
            </div>

            <div class="form-group">
                <label for="description">Description</label>
                <textarea id="description" name="description" required><%= scheme.description %></textarea>
            </div>

            <button type="submit">Update Scheme</button>
        </form>

        <div class="back-link">
            <a href="/admin/dashboard">← Back to Admin Dashboard</a>
        </div>
    </div>
</body>
</html>